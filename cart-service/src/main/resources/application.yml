server:
  port: 8092
spring:
  application:
    name: cart-service
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/uhg-sample-backend?createDatabaseIfNotExist=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true